#!/usr/bin/env python

from setuptools import setup

setup(
    # Metadata
    name='CFQ',
    version='0.0.1',
    description='A package for storing rational numbers in continued-fraction'
                ' format',
    author='CrepeGoat',
    # Contents
    packages=['cfq'],
    # Dependencies
    #install_requires=['sortedcontainers'],
    #tests_requires=['pytest'],
)
